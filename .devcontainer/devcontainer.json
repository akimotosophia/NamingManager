{
  "name": "naming-generator",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "forwardPorts": [3000], 
  "postCreateCommand": "npm install -g create-react-app",
  "remoteUser": "root"
}
